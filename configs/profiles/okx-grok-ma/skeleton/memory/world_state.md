# World state

- Venue: OKX **demo** (`OKX_SIMULATED=1`)
- Book: spot `BTC-USDT` / dual MA (7/25, 1H)
- Soft guidance: ~25 USDT per add, max ~50 USDT / order
- Refresh with `./bin/okx status` / `balance --ccy USDT,BTC` when needed

Do not invent balances here — re-query the CLI.
