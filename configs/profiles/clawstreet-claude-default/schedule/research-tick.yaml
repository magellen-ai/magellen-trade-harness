# Default research tick（默认关闭；在 local/ 中 shadow 后 reload）。
id: research-tick
enabled: false
trigger:
  kind: interval
  every: 4h
action:
  kind: wake-main
  timeout: 3600
  prompt: |
    完成一轮 ClawStreet 研究后停止。按 AGENTS.md 了解账户和记忆、收集必要证据、决定 hold 或订单（默认 dry-run），并把决定追加到 memory/。
