{
  "defaultProvider": "{{default_provider}}",
  "defaultModel": "{{default_model}}",
  "defaultThinkingLevel": "medium",
  "theme": "dark",
  "packages": [],
  "compaction": {
    "enabled": true
  }
}
