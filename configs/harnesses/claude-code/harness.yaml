# Thin harness binding: how to talk to Claude Code.
# Experiment differences (CLAUDE.md, models, skills) live in configs/profiles/.
skill_dirs:
  - .claude/skills
wrappers:
  - clawstreet
  - harness
env_from: cc-env
materialize:
  - src: settings.json
    dest: .claude/settings.json
launch:
  argv:
    - claude
    - --setting-sources
    - project,local
  sync_before: true
# Default wake binding; profiles may override schedule.actions entirely.
# context_files_mode: Claude expands @path inside the -p prompt string.
schedule_actions:
  wake-main:
    cwd: instance
    context_files_mode: prompt_prefix
    defaults:
      max_turns: "30"
    before:
      - argv:
          - uv
          - run
          - harness
          - instance
          - sync-settings
          - "{instance}"
    argv:
      - claude
      - -p
      - "{prompt}"
      - --setting-sources
      - project,local
      - --permission-mode
      - dontAsk
      - --max-turns
      - "{max_turns}"
