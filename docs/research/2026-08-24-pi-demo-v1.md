# PI demo v1 — memory + search + schedule

日期：2026-08-24  
范围：`clawstreet-pi-default` + 实例 `instances/pi-test`  
目标：把「长任务记忆 / 信息搜索 skill / 真实定时」接到同一条可 dry-run 的研究环路上。

## 闭环

```
existing (portfolio + memory)
  → external (quote / optional Tavily)
  → decide
  → dry-run order (platform rate limits only)
  → write memory/journal
```

## 交付物

| 件 | 路径 |
|----|------|
| Search skill | `skills/investment-search/` (`quote.py`, `search.py`) |
| Schedule pack | `configs/schedules/pi-demo/` |
| Profile | `configs/profiles/clawstreet-pi-default/`（skills + AGENTS 决策环） |

## 怎么测

**主路径（手工强制唤醒，推荐）：**

```bash
uv run harness schedule apply pi-test --pack pi-demo
uv run harness schedule tick pi-test --rule research-tick
```

`--rule` 会忽略 `enabled: false` 与 interval，适合反复干跑。

**真实定时（只测一次 5m）：**

1. `local/timer-smoke.yaml`：`interval: 5m` + `script` 写 marker（避免为测时钟浪费 LLM）
2. crontab 每分钟 `harness schedule tick pi-test`
3. 首 tick 立即 FIRED；约 5 分钟后再 FIRED 一次即够
4. 将 `timer-smoke` 设 `enabled: false` 并 `reload`；日常仍用 `--rule research-tick`

生产研究规则 `research-tick` 默认 disabled @ 4h，需要时再 local shadow 打开。

## 约束

- 默认 dry-run；`--live` 需 `memory/risks.md` 未过期授权
- 无本地日订单上限（与官方一致）；遵守平台频率限制（约 60 req/min；`429` 退避）
- 无 `TAVILY_API_KEY` 时跳过网页搜，用 quote + memory 继续
- 密钥不进 git / chat / 明文日志
